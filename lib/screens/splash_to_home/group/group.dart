import 'package:flutter/material.dart';

class CollegeGroup extends StatelessWidget {
  static String routeName = './group';
  const CollegeGroup({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
